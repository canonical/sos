name: Main Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_run:
    workflows: [ "Upstream Sync" ]
    types: [ "completed" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      distros: ${{ steps.set-matrix.outputs.distros }}
      builds: ${{ steps.set-matrix.outputs.builds }}
    steps:
      - id: set-matrix
        # Edit this list to add/remove distros for the ENTIRE pipeline
        run: |
          echo "distros=[\"22.04\", \"24.04\"]" >> $GITHUB_OUTPUT
          echo "builds=[\"deb\", \"snap\"]" >> $GITHUB_OUTPUT

  filters:
    runs-on: ubuntu-latest
    outputs:
      manpage: ${{ steps.filter.outputs.manpage }}
      foreman: ${{ steps.filter.outputs.foreman }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            foreman:
              - '.github/workflows/**'
              - '**/__init__.py'
              - '**/apache.py'
              - '**/foreman_tests.py'
              - '**/candlepin.py'
              - '**/pulp.py'
              - '**/pulpcore.py'
              - '**/foreman_setup.sh'
            manpage:
              - '!man/**'

  py_tests:
    strategy:
      matrix:
        py_version: [ '3.9', '3.14' ]
    uses: ./.github/workflows/py-tests.yaml
    with:
      py_version: ${{ matrix.py_version }}

  build_deb:
    needs: setup
    strategy:
      matrix:
        distro: ${{ fromJson(needs.setup.outputs.distros) }}
    uses: ./.github/workflows/build-deb.yaml
    with:
      distro: ${{ matrix.distro }}

  build_snap:
    needs: setup
    uses: ./.github/workflows/build-snap.yaml

  flake8:
    uses: ./.github/workflows/tox.yaml
    with:
      test_name: flake8

  pylint:
    uses: ./.github/workflows/tox.yaml
    with:
      test_name: pylint

  unit:
    needs: [setup, build_deb, build_snap]
    strategy:
      matrix:
        distro: ${{ fromJson(needs.setup.outputs.distros) }}
        build: ${{ fromJson(needs.setup.outputs.builds) }}
    uses: ./.github/workflows/stage-tests.yaml
    with:
      distro: ${{ matrix.distro }}
      build: ${{ matrix.build }}
      test_name: "unit_tests"
      stage_name: "Unit"

  stage_one:
    needs: [setup, build_deb, build_snap]
    strategy:
      matrix:
        distro: ${{ fromJson(needs.setup.outputs.distros) }}
        build: ${{ fromJson(needs.setup.outputs.builds) }}
    uses: ./.github/workflows/stage-tests.yaml
    with:
      distro: ${{ matrix.distro }}
      build: ${{ matrix.build }}
      test_name: "stageone_tests"
      stage_name: "Stage One"
    secrets: inherit

  stage_two:
    needs: [setup, build_deb, build_snap]
    strategy:
      matrix:
        distro: ${{ fromJson(needs.setup.outputs.distros) }}
        build: ${{ fromJson(needs.setup.outputs.builds) }}
    uses: ./.github/workflows/stage-tests.yaml
    with:
      distro: ${{ matrix.distro }}
      build: ${{ matrix.build }}
      test_name: "stagetwo_tests"
      stage_name: "Stage Two"
    secrets: inherit

  foreman:
    needs: [setup, filters, build_deb]
    if: needs.filters.outputs.foreman == 'true'
    strategy:
      matrix:
        distro: [ "22.04" ]
        build: [ "deb" ]
    uses: ./.github/workflows/stage-tests.yaml
    with:
      distro: ${{ matrix.distro }}
      build: ${{ matrix.build }}
      extra_script: ./tests/test_data/foreman_setup.sh
      test_name: "foreman_tests"
      stage_name: "Foreman"
      foreman_ver: "3.15"
      katello_ver: "4.17"
    secrets: inherit
